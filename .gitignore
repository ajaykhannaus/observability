# Secrets — never commit real credentials
.env
.env.azure
.env.azure.*
azure/bootstrap-azure.env

# macOS metadata
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# Pytest / coverage
.pytest_cache/
.coverage
htmlcov/

# Prometheus local data
/tmp/prom-data/
data/

# Azure Functions local settings (may contain secrets)
function_app/local.settings.json

# Rendered Container App YAML (substituted at deploy time, may contain secrets)
infra/*.rendered.yaml

# IDE / local tooling
.idea/
.vscode/
.claude/
*.swp
*.swo

# Screenshots / scratch captures (don't belong in the repo)
ScreenShot*.png
Screenshot*.png
docs/errors.txt
