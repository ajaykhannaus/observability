# Tempo image with local-storage config baked in for Azure Container Apps.
ARG TEMPO_BASE=grafana/tempo:2.6.1
FROM ${TEMPO_BASE}

COPY infra/tempo-local-config.yaml /etc/tempo/tempo.yaml

EXPOSE 3200 4317
